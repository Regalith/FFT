using UnityEngine;
using System.Collections;

public class lightController : MonoBehaviour {

	public float pulseIntensity;

	void Start () 
	{
		//light1.light.enabled = true;
		//light2.light.enabled = true;
	}
	

	void Update () 
	{/*
		if (light1.light.intensity > 0) 
		{
				light1.light.intensity -= 2 * Time.deltaTime;
				light2.light.intensity -= 2 * Time.deltaTime;
		}
	}
	public void Pulse() 
	{
		light1.light.intensity = pulseIntensity;
		light2.light.intensity = pulseIntensity;*/
	}
}
